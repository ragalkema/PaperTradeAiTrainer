# Model artifacts

Files under `checkpoints/`, `trained/`, and `exported/` are intentionally ignored. Store model artifacts in a versioned registry or S3-compatible object store and record their URI, checksum, training window, feature version, hyperparameters, seed, and code commit.
