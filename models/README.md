# Model artifacts

Trained model binaries and checkpoints are intentionally ignored. Store them in a versioned artifact registry or S3-compatible object store and record their URI, checksum, training window, feature version, hyperparameters, seed, and code commit.
