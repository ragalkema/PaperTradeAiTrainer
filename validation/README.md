# Root validation

Validation evaluates whether real boundaries, environment, schemas, and system states are trustworthy. Run `scripts/validate.ps1` or `scripts/validate.sh`. It intentionally remains separate from tests, which verify expected code behavior.
