# Experiment outputs

Small reviewable configurations belong in `configs/`. Generated results and reports are ignored. Future metadata records experiment, bot, model, dataset and feature versions; chronological train/validation/test windows; hyperparameters; seed; starting balance; financial metrics; fees/trades; and Git commit.
