# Migrations

The baseline research-persistence revision creates PaperTrading-owned session, bot, portfolio,
position, trade, decision, and experiment tables. Financial columns use `NUMERIC(38, 18)`.
