# AiTrainer validation

Future validation covers dataset ordering/gaps/NaNs, feature ranges and point-in-time correctness, model input/output compatibility, experiment reproducibility, and leakage. Financial time-series leakage is a first-class failure, not a minor data-quality warning.
