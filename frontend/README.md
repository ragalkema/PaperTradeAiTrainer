# Frontend

Minimal React, TypeScript, and Vite foundation for the future monitoring dashboard.

Run `npm install`, then `npm run dev`. Quality checks are `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`.
