# Backend

FastAPI and typed domain contracts for the paper-trading platform. This package deliberately contains no real-order execution path.

From this directory, install with `python -m pip install -e ".[dev]"`, run `uvicorn app.main:app --reload`, and test with `pytest`.
