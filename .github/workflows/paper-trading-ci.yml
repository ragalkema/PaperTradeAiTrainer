name: PaperTrading CI

on:
  push:
    branches: [main, dev]
    paths: ["PaperTrading/**", "shared/**", "pyproject.toml", ".github/workflows/paper-trading-ci.yml"]
  pull_request:
    paths: ["PaperTrading/**", "shared/**", "pyproject.toml", ".github/workflows/paper-trading-ci.yml"]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml
      - run: python -m pip install -e ".[dev]"
      - run: ruff check PaperTrading shared
      - run: ruff format --check PaperTrading shared
      - run: mypy PaperTrading/src shared/src
      - run: pytest PaperTrading/tests shared/tests --no-cov
