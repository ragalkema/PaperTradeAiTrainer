# Local data

`raw/` contains immutable source captures; `processed/` contains reproducible normalized or feature data. Their contents are ignored by Git. Large datasets belong in local/object storage with checksums, versions, and provenance tracked in experiment metadata.
