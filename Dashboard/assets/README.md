# Dashboard assets

`icons/` is reserved for the Windows `.ico` application icon and `images/` for original,
redistributable artwork. The current build uses Qt's native icon when no `app.ico` is present.
