# Validation

Tests ask: “Does our code behave as expected?” Validation asks: “Can this actual input, configuration, output, or state be trusted?”

Validation is organized by bounded context and can eventually answer whether datasets, MarketState values, simulations, experiments, schemas, and environment configuration are trustworthy. Run it separately with `scripts/validate.ps1` or `scripts/validate.sh`.
